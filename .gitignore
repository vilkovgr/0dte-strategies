__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store
.vscode/
*.egg-info/

# Large data files tracked via Git LFS or shipped separately
# (the parquet/h5 files in data/ ARE tracked -- they are the shipped panels)

# Raw API downloads (Tier 2 — not committed, rebuilt from source)
data/raw/

# Core analysis dependencies (Tier 1)
# Pinned upper bounds to ensure reproducibility across fresh installs.
numpy>=1.24,<3
pandas>=2.0,<3
matplotlib>=3.7,<4
statsmodels>=0.14,<1
scipy>=1.10,<2
scikit-learn>=1.3,<2
pyarrow>=12.0,<18
h5py>=3.8,<4

# Tier 2: raw data ingest (optional — only needed for rebuild from source)
requests>=2.28,<3
python-dotenv>=1.0,<2
